（1）shift 右键   在此打开命令窗口
（2）node -v  看到版本号就安装成功
 (3) 输入node 回车，   写自己要写的内容，比如consog.log("zhufeng")就会被打印出来；
（4） 连续按两次ctrl C  就会推出这个交互环境
 
（5）进入f盘 就是 f:
（6）也可以执行代码求和 输入node move.js 结果就会出来
（7）__dirname可以查看的，自己的当前路径
（8）__firmane,查看的是文件的名字

引入模块
（1）require("./aaa.js");   //把同级的js，aaa.js引入进来了
（2）模块中的功能，（如函数变量）通过赋给exports对象的某个属性提供给调用者使用

包的概念；可以理解他就是个文件夹